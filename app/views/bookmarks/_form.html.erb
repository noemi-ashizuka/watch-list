<%= simple_form_for [@list, @bookmark], id: "bookmark_form", data: { controller: 'post-form' } do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment, as: :text %>
  <%= f.submit class: 'btn btn-blue' %>
<% end %>

<template>
  <turbo-stream target="post_form" action="replace">
  <h2>Replaced</h2>
  <%= simple_form_for [@list, @bookmark], id: "bookmark_form" do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment, as: :text %>
    <%= f.submit class: 'btn btn-blue' %>
  <% end %>
  </turbo-stream>
</template>
